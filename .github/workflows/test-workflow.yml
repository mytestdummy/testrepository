name: Manually triggered workflow
on:
  workflow_dispatch:
    inputs:
      n-remaining-commits:
        description: 'Number of most recent commits remaining'
        required: true
  # schedule:
    # every 28th day of a month at 23:00
    # - cron: '22 13 16 * *'
    # inputs:
    #   n-remaining-commits: 30
    #     description: '{N}umber of most recent commits remaining'
    #     required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        token: ${{ secrets.YOUR_TOKEN }}
        fetch-depth: '0'
    - uses: mytestdummy/testaction@master
      with:
        n-remaining-commits: ${{ github.event.inputs.n-remaining-commits }}

# RESIDUUM
 # jobs:
  # build:
    # runs-on: ubuntu-latest
    # steps:
    # - uses: actions/checkout@master
      # # with:
      # #   repository: 'sitdisch/cloud'
      # #   persist-credentials: false
    # - name: Clean Process
      # run: |
    # - name: Commit Cleaning
      # run: |
      #   git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #   git config --local user.name "github-actions[bot]"
      #   git add .
      #   git commit -am "Repository History Cleaned | Monthly"
    # - name: Push Results
      # uses: ad-m/github-push-action@master
      # # with:
      # #   repository: 'sitdisch/cloud'
      #   # github_token: ${{ secrets.SITDISCH_WORKFLOW_REPO_TOKEN }}
